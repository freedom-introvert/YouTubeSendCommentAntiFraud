<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.YTSendCommAntiFraud" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/yt_theme_color</item>
        <item name="colorPrimaryDark">@color/yt_theme_color</item>
        <item name="titleTextColor">@color/white</item>
        <item name="windowActionBar">true</item>
        <item name="colorAccent">@color/yt_theme_color</item>
    </style>

    <style name="Theme.YTSendCommAntiFraud.HasActionBar" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/yt_theme_color</item>
        <item name="colorPrimaryDark">@color/yt_theme_color</item>
        <item name="titleTextColor">@color/white</item>
        <item name="windowActionBar">true</item>
        <item name="colorAccent">@color/yt_theme_color</item>
    </style>


    <style name="Theme.YTSendCommAntiFraud" parent="Base.Theme.YTSendCommAntiFraud" />

    <style name="dialog_activity" parent="@style/Theme.AppCompat.DayNight.Dialog">
        <!-- 去黑边 -->
        <item name="android:windowFrame">@null</item>
        <!-- 设置是否可滑动 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 设置是否透明 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 背景 -->
        <item name="android:background">@null</item>
        <!-- 窗口背景 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 是否变暗 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 点击空白部分activity不消失 -->
        <item name="android:windowCloseOnTouchOutside">true</item>
        <!-- 无标题 有的手机设置这行代码-->
        <item name="windowNoTitle">true</item>

        <item name="colorAccent">@color/yt_theme_color</item>

    </style>
</resources>